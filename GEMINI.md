# Webエンジニア実務チェックリスト プロジェクト

## 目的

AI時代でも長期的に価値を出し続けられるWebエンジニアになるための、実務ベースの学習ロードマップを作成する。
最終的な目標は、1000〜1500項目規模の実務チェックリストを完成させること。

## プロジェクト構造

- `docs/`: チェックリスト本体のMarkdownファイルを格納するディレクトリ。
- `artifacts/`: 各チェック項目の学習成果物（まとめ、Ankiカード、サンプルコード）を格納するディレクトリ。

## 作業方針

1.  `docs/` 内の各カテゴリファイル（`01_Requirements.md`など）のチェック項目を、それぞれ50項目程度まで拡充していく。
2.  ユーザーの指示があれば、優先的に指定されたカテゴリの拡充を行う。指示がなければ、番号順に進める。
3.  すべての作業内容はGitでバージョン管理する。
4.  チェック項目を完了した際は、`[x]` のようにチェックを入れると同時に、行末に `(YYYY-MM-DD)` の形式で完了日を記録する。

## 現在の進捗 (2026-07-18)

- [x] `docs/` ディレクトリと、13カテゴリ分のMarkdownファイルを全て拡充し、合計1006項目のチェックリストを達成。
- [x] `README.md`に学習計画のモデルケースを追記。
- [x] `artifacts/` ディレクトリに、学習成果物を格納するためのカテゴリ別フォルダを作成。
- [x] 完了日を記録するルールを `GEMINI.md` と `README.md` に追記。
- [x] DGN-001を完了し、完了日を記録。

## 次のタスク

- チェックリストに基づいた学習の開始。
